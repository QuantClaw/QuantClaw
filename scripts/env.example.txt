# QuantClaw Environment Variables
#
# Copy relevant variables to your shell profile (~/.bashrc, ~/.zshrc)
# or to a .env file loaded by your process manager.
#
# None of these are required — QuantClaw works without them,
# falling back through the cascade where applicable.

# ---------------------------------------------------------------------------
# Provider credentials (optional)
#
# Provider registry resolves keys in this order:
# 1) explicit config value
# 2) explicit config env key (apiKeyEnv)
# 3) convention envs: <PROVIDER>_API_KEY, <PROVIDER>_KEY, <PROVIDER>_TOKEN
# ---------------------------------------------------------------------------

# OpenAI-compatible provider
OPENAI_API_KEY=

# Anthropic provider
ANTHROPIC_API_KEY=

# GitHub Copilot provider (any one of these works)
GITHUB_COPILOT_TOKEN=
GITHUB_COPILOT_API_KEY=

# Optional gateway auth token override (used by CLI/gateway flows)
QUANTCLAW_AUTH_TOKEN=

# ---------------------------------------------------------------------------
# Web Search (tools: web_search)
# Cascade order: Brave → Tavily → Perplexity → DuckDuckGo → Grok
# DuckDuckGo requires no key and is always tried before Grok.
# ---------------------------------------------------------------------------

# Brave Search API — https://api.search.brave.com
BRAVE_API_KEY=

# Tavily Search API — https://tavily.com
TAVILY_API_KEY=

# Perplexity Sonar API — https://www.perplexity.ai
PERPLEXITY_API_KEY=

# xAI Grok API — https://x.ai
XAI_API_KEY=

# ---------------------------------------------------------------------------
# OKX Agent TradeKit (MCP trading server)
# Obtain via OAuth at https://app.okx.com/en-us/agent-tradekit
# MCP endpoint: https://us.okx.com/api/v1/mcp/trading-oauth
# ---------------------------------------------------------------------------

# OAuth access token (required — passed as Authorization: Bearer <token>)
OKX_ACCESS_TOKEN=

# Optional: OKX REST API credentials for direct API usage
OKX_API_KEY=
OKX_SECRET_KEY=
OKX_PASSPHRASE=
